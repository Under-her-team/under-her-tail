event_inherited();
text = text0;
	Dialog_Add(text); //Loads textbox with "text"
	Dialog_Start(); //Ensures text is printed when player first interacts with this object.
text = text1;
	Dialog_Add(text); //loads "text" with whatever was written for text0 and so on.
	Dialog_Start(); //Prints text out into the textbox for player to read.
text = text2;
	Dialog_Add(text); //loads "text" with whatever was written for text0 and so on.
	Dialog_Start(); //Prints text out into the textbox for player to read.